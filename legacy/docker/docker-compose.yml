services:
  georgian-pdf-ocr:
    build:
      context: ../..
      dockerfile: legacy/docker/Dockerfile
    container_name: georgian-pdf-ocr
    ports:
      - "8765:8765"
    restart: unless-stopped
